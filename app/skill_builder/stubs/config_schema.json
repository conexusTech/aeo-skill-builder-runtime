{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://conqrse.dev/schemas/skill-builder-config/v1.json",
  "title": "Skill Builder Config (v1 — RATIFIED 2026-08-03)",
  "description": "Versioned contract for the skill config the Conversational Skill Builder agent emits and the generic prospect-scanner runtime executes. PRD R7. Owned by aeo-backend (enforced at STATE_DELTA persist / test-run start / finalize); the agent-service builder + skills-service scanner consume a version-pinned copy. This v1 locks the ENVELOPE + cross-cutting invariants (version, catalog metadata, run_parameters, pipeline stages, org-agnostic context refs) and stays permissive INSIDE each domain section (additionalProperties:true) so domain internals can be co-designed with the runtime workstream without a schema round-trip. VOCABULARY (ruled 2026-08-03, see $comment): 'section' = authoring unit, 'phase' = runtime execution unit. Never use 'phase' for the five authoring sections.",
  "$comment_partial_drafts": "OMIT WHAT YOU DON'T KNOW YET — NEVER PLACEHOLDER IT. Structural/incremental mode drops top-level `required` so a half-built draft persists fine, but `const`, `enum` and type constraints are PROPERTY constraints and still apply from the first STATE_SNAPSHOT. That is deliberate, not an oversight: relaxing them would leave structural mode validating essentially nothing, and a placeholder that passes validation is worse than an absent field, because it reaches finalize looking legitimate. So a mid-conversation draft should carry `version: \"1.0\"` (known immediately), omit `type` until the skill type is actually decided, and omit any section not yet proposed — rather than emitting `type: \"prospect-scanner\"` or a draft version string. Flagged by aeo-agent-service 2026-08-03, whose skeleton did exactly that and would therefore have been rejected on turn one at the STATE_DELTA persist point rather than at the finalize gate where it would have been noticed in testing.",
  "$comment": "SECTION vs PHASE — the one naming rule in this contract. A **section** is an AUTHORING unit: one of the five fixed conversation steps (geography, discovery, validation, contacts, scoring) that the builder chat proposes and the user accepts individually, and that STATE_DELTA JSON pointers address. A **phase** is a RUNTIME EXECUTION unit: an ordered scanner step id in `execution_phases`, whose subsets drive reduced test runs (R6). They are different cardinalities, different lifecycles and different owners, and all three repos had independently used the word 'phase' for both — aeo-frontend and aeo-agent-service for the five authoring sections, this schema for the execution ids. Ruled 2026-08-03: the field formerly named `phases` is now `execution_phases`; the five authoring sections keep their existing top-level key names so neither sibling has to change emitted paths or STATE_DELTA pointers. Do not reintroduce a top-level `phases`.",
  "type": "object",
  "required": ["version", "run_parameters"],
  "$comment_type_not_required": "`type` was REMOVED from `required` on 2026-08-07, and this is the second time this exact defect has shipped. The agent is the only config emitter and it never writes `type`: `draft.skeleton()` takes `type_` but its one caller never passes it, and `config[\"type\"]` is assigned nowhere else in the runtime — deliberately, because the field is enum-constrained and their ratified authoring rule is 'omit what isn't known yet, never placeholder it'. With `type` required, STRICT mode rejected every draft, which is BOTH gates (test-run start and finalize), forever. Identical to the `execution_phases` defect corrected in thread #07.1 for the identical reason, on a neighbouring field. It survived because R2 did not exist, so no real config had ever reached a strict gate. Nothing needs the value: finalize already falls back to `SkillType.Customer` when it is absent, and R13's `skill_type` dimension is read from the `skills.type` COLUMN, not from here. Found by running the real container, not by a test — the pattern holds.",
  "properties": {
    "version": {
      "description": "Schema CONTRACT version — not the draft's own revision. It is `1.0` from the very first STATE_SNAPSHOT of a brand-new draft, because it identifies which contract the document speaks, and that is known before any content is. Do not put a draft/authoring version here (e.g. `0.1.0-draft`); there is no field for one, and `const` means it fails even in structural mode.",
      "const": "1.0"
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 255,
      "description": "Catalog display name."
    },
    "slug": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "maxLength": 100,
      "description": "Catalog slug. Convention: <vertical>-prospect-scanner (Q2). Distinct from runtime_slug (dispatch pointer)."
    },
    "type": {
      "enum": ["customer", "project"],
      "description": "Skill type — mirrors skills.type. This IS R13's `skill_type` match dimension; it is not a separate field. Do not emit a `skill_type` key."
    },
    "product_description": {
      "type": ["string", "null"],
      "description": "snake_case, like every key in this contract. `productDescription` is NOT accepted — see the root $comment on casing."
    },
    "vertical": {
      "type": "string",
      "description": "R13 match dimension 1 of 3. Vertical/industry tag powering library-first catalog match. Curated taxonomy still pending an owner (Q8), so no enum yet — a free string today."
    },
    "lead_type": {
      "enum": ["A", "B", "MIXED"],
      "description": "R13 match dimension 2 of 3. Mirrors the `organization_lead_type_enum` / `organizations.lead_type` vocabulary exactly — uppercase, `MIXED` not `mixed`. Declared 2026-08-03: aeo-agent-service was already emitting this and it was being accepted-but-unvalidated, which is precisely the trust problem R13 cannot have, since its whole premise is that catalog metadata is reliable."
    },
    "run_parameters": {
      "type": "object",
      "description": "JSON Schema for the per-scan parameters body (validated by RunParametersValidator at scan time). Must itself be a compilable JSON Schema — enforced in strict mode."
    },
    "execution_phases": {
      "type": "array",
      "items": { "type": "string", "enum": ["zip_discovery", "discovery", "validation", "contacts", "scoring"] },
      "minItems": 1,
      "description": "RUNTIME concept, and OPTIONAL — omit it and the scanner runs its full built-in phase order. Only supply it to run a reduced subset, which is what drives test runs (R6). Ordered execution phase ids; the scanner runtime honours exactly the five enumerated in `items.enum`, in that order. Renamed from `phases` on 2026-08-03 to end the collision with the five authoring sections — see the root $comment. Not authored in the chat and not shown per-section in the draft panel; `RuntimeContextService` strips it before the builder agent sees the config, because the agent authors sections, not runtime phases.",
      "$comment": "NOT in `required`, deliberately — corrected 2026-08-03 after aeo-agent-service confirmed they never populate it. It was required in the first cut, which would have made strict-mode finalize reject every draft the agent produces, since the only party emitting configs does not write this field. Omission is also already the normal case for scans elsewhere in the gateway: scan-runs.service.ts only sets `phases` on the job when the Strategy Tester passes an explicit subset, otherwise 'the scan task runs its built-in phases'. If a future scanner genuinely needs an explicit list, the gateway supplies it at dispatch — not the authoring agent. 🔨 CORRECTION 2026-08-05: this description previously gave the example ids as phase-0/phase-1a/phase-2/phase-3. Those were never real — the legacy AV engine does not read the phase subset at all, and configurable-prospect-scanner ignores names it does not recognise (force-including discovery), so a subset spelled that way silently ran discovery only. The five real names are now enumerated in `items.enum`, transcribed from ALL_PHASES in configurable-prospect-scanner/aeo/runner.py. Nothing can import that Python list, so when the scanner adds or renames a phase, the same PR must touch this enum and src/backend/skills/config/scanner-phases.ts — a gateway-side drift guard locks those two to each other."
    },
    "geography": {
      "allOf": [{ "$ref": "#/$defs/section" }],
      "description": "AUTHORING section 1 of 5. Geographic scope / home-market targeting. Org-specific values MUST be context refs (see $defs/contextRef, R12). Maps to runtime phase: zip discovery.",
      "properties": {
        "home_markets": { "$ref": "#/$defs/boundStringList" },
        "secondary_markets": { "$ref": "#/$defs/boundStringList" },
        "excluded_markets": { "$ref": "#/$defs/boundStringList" },
        "targeting": { "$ref": "#/$defs/geographyTargeting" }
      }
    },
    "discovery": {
      "allOf": [{ "$ref": "#/$defs/section" }],
      "description": "AUTHORING section 2 of 5. Discovery rules — sources, queries, ICP seeds. Maps to runtime phase: discovery sweep.",
      "properties": {
        "sources": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": { "$ref": "#/$defs/discoverySource" },
          "description": "Named discovery buckets. Keys are free-form bucket names; each value carries the engine's own source shape verbatim. This is the ONE part of `discovery` the scanner reads — `icp_attributes` below is currently unconsumed."
        },
        "lookalike_sources": { "$ref": "#/$defs/boundStringList" },
        "icp_attributes": { "$ref": "#/$defs/boundStringList" },
        "entries_per_query": {
          "type": "integer",
          "minimum": 1,
          "maximum": 25,
          "description": "How many candidate companies each search query should return. Raise it when a vertical is dense enough that each search can name many good candidates; keep it low when searches return a few strong ones and a long tail of noise. Raising it multiplies search cost by the same factor. Omit it to accept the runtime default.",
          "$comment_ordering": "Sentence order is load-bearing, not style. aeo-agent-service renders descriptions into the authoring prompt through a _NOTE_BUDGET that truncated the first version at 327 of 501 chars — dropping the fourth sentence, which was the only one telling the model WHEN to reach for the knob. It learned what the control does and that it costs money, but not how to choose. Reordered so the decision guidance comes second and the whole description fits inside any plausible budget. The yield arithmetic that used to live here is in `$comment_why_declared` below: explanatory, not decision-guiding, so it does not need to reach the model.",
          "$comment_why_declared": "Added 2026-08-12. The scanner has always had this knob but it lived in the provider block alongside model and retry policy, was documented as a deployment concern, and was therefore unauthorable. It was the binding cap on every scan: two consecutive production runs returned identical counts (13 expanded queries x 3 entries = 39 raw, 36 after dedup) from largely different companies, so the prospect count was measuring configuration rather than the market. Declared here as an OPTIONAL key, which is pure relaxation and needs no version bump — same call as `custom_modules`. NOTE for aeo-agent-service: this will trip the pinned-copy drift test; re-pin, and teach the key so an operator can actually set it.",
          "$comment_default": "The runtime default is deliberately unchanged and low. Raising it is a spend decision, not a schema decision, and it belongs to whoever owns the search budget."
        },
        "max_prospects": {
          "type": "integer",
          "minimum": 1,
          "maximum": 1000,
          "description": "Hard ceiling on how many prospects one run may produce. Every prospect kept costs two grounded model calls after discovery — one to verify its location, one to judge it — so this is the knob that decides how long a run takes. Set it to the number of leads a person will actually work through. Omit it to accept every prospect discovery finds, which is unbounded.",
          "$comment_why_declared": "Added 2026-08-14. There was NO working per-run cap anywhere, in this schema or in the scanner, and three separate things looked like one: `discovery.target_prospects` is a FLOOR (the geo loop reads it only to decide whether to run another round, and verifies every candidate of the round it has first), `SCANNER_TOP_N`'s documented ranking cut lives in `FileSink.close()` which the AEO path never calls, and `contacts.max_prospects` bounds only contact enrichment, downstream of both expensive phases. Measured cost: production run 0a85e4bd discovered 262 prospects in 11 minutes, spent 49 minutes on per-prospect location verification at concurrency 2, and was killed by its one-hour job deadline having validated, scored and enriched nothing — 262 rows persisted with every counter on the run left NULL.",
          "$comment_semantics": "CUMULATIVE across discovery rounds, not per round — `max_discovery_rounds` defaults to 2, so a per-round ceiling of 100 would permit 200. Enforced BEFORE any prospect is persisted, because AEO writes prospects `ON CONFLICT (id) DO NOTHING` and a posted row can never be retracted; the scanner therefore intercepts the engine's `prospects` event rather than post-filtering. Selection is multi-source-first then round-robin by discovery source, so a ceiling never silently reduces a four-source recipe to one — it is a volume control, NOT a shortlist, since ranking needs scores and scoring runs after discovery.",
          "$comment_no_env_fallback": "NOTE for aeo-agent-service: unlike `target_prospects` and `contacts.max_prospects`, this key deliberately does NOT fall back to `SCANNER_TOP_N`. That variable is `1` in production, so a fallback would cap every run at a single prospect while looking like a sensible default — and the symptom (one prospect, no error) is indistinguishable from a market with nothing in it."
        },
        "target_prospects": {
          "type": "integer",
          "minimum": 1,
          "description": "How many in-area prospects to keep searching FOR — a floor, not a ceiling. Discovery runs another round while it has fewer than this; it never stops a round early or discards anything to stay under it. To LIMIT a run, use `max_prospects`.",
          "$comment_why_declared": "Declared 2026-08-14 as a TRAP, not as a feature. The scanner has always read this key (`runner.py`, `_config_limit(config, 'discovery', 'target_prospects')`) while the schema never declared it, and sections are `additionalProperties: true`, so an author could write it, watch it validate, and get the opposite of what the name promises. It is the sufficiency target of the geographic verify loop: `geo_loop.discover_in_area` compares it against the in-area count to decide whether to run another sweep, and that comparison happens AFTER the round it already has is fully verified. On the run that motivated `max_prospects` it was unset and therefore fell back to `SCANNER_TOP_N=1` — a target of ONE — and the run still discovered and verified 262 prospects, because a floor cannot cap anything. Declared so the description can say so where an author will read it."
        }
      }
    },
    "validation": {
      "allOf": [{ "$ref": "#/$defs/section" }],
      "description": "AUTHORING section 3 of 5. Signal validation rules — disqualifiers, required signals. Maps to runtime phase: signal validation.",
      "properties": {
        "in_market_signals": { "$ref": "#/$defs/boundStringList" },
        "disqualifiers": { "$ref": "#/$defs/boundStringList" }
      },
      "$comment": "Shape ratified 2026-08-04 (thread #17) against what `aeo/phases/validation.py` actually reads, not by design-by-committee. Both fields are R12-bound positions."
    },
    "contacts": {
      "allOf": [{ "$ref": "#/$defs/section" }],
      "description": "AUTHORING section 4 of 5. Decision-maker search — target titles, contact enrichment. Maps to runtime phase: decision-maker search.",
      "properties": {
        "titles": { "$ref": "#/$defs/boundStringList" },
        "seniorities": { "$ref": "#/$defs/boundStringList" }
      },
      "$comment": "Shape ratified 2026-08-04 (thread #17) against what `aeo/phases/contacts.py` actually reads. Both fields are R12-bound positions; note the context-field keys are `decision_titles` / `decision_seniorities`, which do NOT match the config key names — see context-field-keys.json's config_positions."
    },
    "scoring": {
      "allOf": [{ "$ref": "#/$defs/section" }],
      "description": "AUTHORING section 5 of 5. Scoring model — weights, emphasis. Org-specific emphasis MUST be context refs (R12). Maps to runtime phase: scoring.",
      "properties": {
        "factors": {
          "type": "array",
          "description": "The ICP-fit axis, and the largest single contributor to a score. Each entry names a field this config's own discovery sources collect; a value being present earns full credit, unless min/max are given, which turn presence into a numeric threshold. Name a field the sources actually return — a factor naming a field nothing collects scores zero for every prospect, with no error anywhere.",
          "items": {
            "type": "object",
            "required": ["name", "weight"],
            "properties": {
              "name": {
                "type": "string",
                "minLength": 1,
                "description": "The collected field this factor reads. Matched loosely against the field names the discovery sources return."
              },
              "weight": {
                "type": "number",
                "exclusiveMinimum": 0,
                "description": "Relative weight within this axis. Weights are normalized across all factors, so only their ratios matter, not their sum."
              },
              "min": {
                "type": "number",
                "description": "Optional lower bound. The first number found in the collected value must be at least this, or the factor earns nothing. Only meaningful for fields whose values are numeric."
              },
              "max": {
                "type": "number",
                "description": "Optional upper bound, applied the same way as min."
              },
              "description": {
                "type": "string",
                "description": "Why this factor predicts fit. Authoring notes only — never read by the scoring engine."
              }
            },
            "additionalProperties": true
          }
        },
        "disqualify_below": {
          "type": "number",
          "description": "Optional score floor. Prospects below it are FLAGGED, never removed — the threshold is chosen before any score distribution has been seen, so it marks a cutoff for review rather than deleting results."
        },
        "score_cap": {
          "type": "integer",
          "minimum": 1,
          "description": "Upper bound on the total score after all axes and any adjustment are summed."
        },
        "ai_adjustment": {
          "type": "object",
          "description": "Bounds on the discretionary per-prospect adjustment, clamped to this range.",
          "properties": { "min": { "type": "number" }, "max": { "type": "number" } }
        },
        "completeness": { "$ref": "#/$defs/scoringAxis" },
        "fit": { "$ref": "#/$defs/scoringAxis" },
        "region_bonus": { "$ref": "#/$defs/scoringAxis" },
        "multi_source": { "$ref": "#/$defs/scoringAxis" },
        "pipeline": { "$ref": "#/$defs/scoringAxis" }
      },
      "$comment": "Declared 2026-08-12 (thread #32), and every property was read off the engine's actual read sites rather than transcribed from the thread: `factors` + min/max at av_lead_scanner.py score_config_factors/_factor_credit, `score_cap`/`ai_adjustment` at :1290-1291, `disqualify_below` at :1365, the five axis `max` values at _DEFAULT_SCORING. WHY THIS EXISTS: `scoring` previously declared nothing, so each knob the engine consumed was invisible to every other repo — aeo-agent-service's drift test compares published files and there was no file difference to detect, which is how the engine's vocabulary went stale in their layer inside four hours. TWO DEVIATIONS FROM THE PROPOSED FRAGMENT, both deliberate: (1) `disqualify_below` was NOT in the proposal and the engine does read it — the same undercount class the thread exists to close, found by checking instead of trusting. (2) `required: [name, weight]` and `exclusiveMinimum: 0` are a TIGHTENING, not the 'pure relaxation' the proposal claimed: the engine defaults a missing weight to 1 and silently SKIPS weight<=0, so both cases are legal today and become errors here. Kept because an implicit weight of 1 beside an explicit 25 is near-certainly not what the operator meant, and a skipped factor is invisible — but it is a behaviour change, argued rather than smuggled. Safe as a non-bump because `factors` is new: no shipped config declares it. The vertical-shaped internals (fit.keyword_scores, pipeline.statuses/phase_fallback, region_bonus.regions/state_aliases, multi_source.tiers, completeness.fields) are deliberately NOT declared — the PO ruled they must go, and completeness.fields is now derived from the config's own sources when unset, so declaring it would invite a model to author what the runtime computes better. KNOWN GAP, evidenced not theorised: a factor cannot express RECENCY. On run b29b3416 a 2023 permit earned the same full credit as one from last month, and min/max cannot fix it because the bound reads the first number in the value — a date yields its year, and the collected formats varied enough ('August 2026', '02/14/2024', '2026-07-30') that any threshold would compare against 2026, 2 and 2026. Do not add a date bound to this shape without a real date parser."
    },
    "pipeline": {
      "type": "object",
      "properties": {
        "stages": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "required": ["key"],
            "properties": {
              "key": { "type": "string", "minLength": 1 },
              "label": { "type": "string" },
              "minMonths": {
                "type": "number",
                "description": "Inclusive lower bound, in whole months from today, of the event date that puts a prospect on this stage. Negative = past. Declare WITH maxMonths or not at all — a rung carrying one bound and not the other is treated as having none."
              },
              "maxMonths": {
                "type": "number",
                "description": "Exclusive upper bound, in whole months from today. Same convention as the project ladder's TIMELINE_STAGES: months >= minMonths && months < maxMonths."
              },
              "requiresContact": {
                "type": "boolean",
                "description": "Only for a rung with no month bounds: restrict it to prospects that have a contact route (email/phone/linkedin). Ignored on a bounded rung."
              },
              "color": {
                "type": "string",
                "description": "Hex badge colour. Optional — a consumer falls back to a palette by declared order, so an operator is never blocked on picking colours."
              },
              "description": {
                "type": "string",
                "description": "One-line explanation of when a prospect belongs here. Rendered as a column tooltip."
              }
            },
            "additionalProperties": true
          }
        },
        "signalFields": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "description": "Discovery field names read as timing signals, in `discovery_data.by_source.*`. Defaults to [trigger_date, transaction_date] when omitted. These are EVENT dates, not completion dates — see $comment_direction."
        }
      },
      "additionalProperties": true,
      "description": "Pipeline stage definitions (customer skills). Validated against prospect pipeline-status writes (skills.config.pipeline.stages[]). Declare stages in the order an operator should read them: the FIRST rung is the entry point and is where a prospect with no signal rests.",
      "$comment_why_declared": "The five per-stage keys beyond key/label were declared 2026-08-21, and they were CONSUMED for one commit before they were declared — which is the exact defect this schema's `scoring` $comment describes: a knob the runtime reads that no other repo can see, with no file difference for aeo-agent-service's pinned-copy drift test to detect. `additionalProperties: true` meant nothing errored, so the builder would have kept emitting {key, label} only, every authored rung would have carried no bands, and EVERY prospect would have rested at the entry rung on a scan reporting success. Silent, and indistinguishable from a vertical that genuinely has no timing signal. All five are OPTIONAL, so this is pure relaxation and needs no version bump — same call as `entries_per_query`. NOTE for aeo-agent-service: this will trip the pinned-copy drift test; re-pin, and teach the keys so a builder-authored vocabulary can actually express timing rather than only naming rungs.",
      "$comment_direction": "THE DIRECTION IS THE TRAP, and it cost a production surface. For a TRIGGER the decision FOLLOWS the date; the scanner's project ladder subtracts a sales cycle as if every date were a project COMPLETION date, which turned 'New location opening, April 30 2026' into 'Decided 4+ months ago' at -16 months when the true offset was -4. Bounds here are read directly off months-from-today. Nothing is subtracted.",
      "$comment_recency_gap_closed": "This closes the KNOWN GAP recorded in the `scoring` $comment: 'a factor cannot express RECENCY... the bound reads the first number in the value — a date yields its year, and the collected formats varied enough (August 2026, 02/14/2024, 2026-07-30) that any threshold would compare against 2026, 2 and 2026. Do not add a date bound to this shape without a real date parser.' A real parser now exists (`customer-pipeline-stage.util.ts`) handling all three of those spellings plus 'April 27, 2023', and REFUSING a bare year rather than inventing a month from adjacent words. minMonths/maxMonths are therefore a date bound with a parser behind them, which is what that instruction asked for. Note it applies to `pipeline.stages`, not to `scoring.factors` — the recency gap on scoring factors is still open."
    },
    "org_input_schema": {
      "description": "Per-org input field declarations. Two accepted shapes (array OR object-map) — normalized by normalizeOrgInputSchema. Kept permissive here.",
      "type": ["array", "object"]
    },
    "custom_modules": {
      "type": "array",
      "description": "PRD R11 — reviewed custom scoring modules. OFF at launch by decision D5: the scanner's loader returns [] unless SCANNER_CUSTOM_MODULES_ENABLED=true, so an absent or empty array is a no-op. Declared here anyway because the shape is already fixed by the scanner's pre-import gate (aeo/modules/loader.py REQUIRED_SPEC_FIELDS) — landing it now is what stops the schema and that gate from disagreeing later. Every field is REQUIRED and the item is CLOSED: a module spec is the input to a security decision, so a missing or misspelled field must be an error, never a default.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "name",
          "path",
          "sha256",
          "api_version",
          "reviewed_by",
          "reviewed_at"
        ],
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "Stable identifier. Namespaces this module's signals (`<name>.<key>`) so two modules cannot overwrite each other, and the loader rejects a module whose own `name` disagrees with this one — otherwise signals would land under the wrong namespace."
          },
          "path": {
            "type": "string",
            "minLength": 1,
            "description": "File path relative to the scanner's module directory. The loader enforces containment; a path escaping that directory is rejected before import."
          },
          "sha256": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$",
            "description": "Hex digest of the file AS REVIEWED. Load-bearing, not hygiene: importing a Python module executes it, so there is no sandbox and the review record is the only real control. Without binding reviewed bytes to loaded bytes, `reviewed_by` attests to whatever the file said at review time."
          },
          "api_version": {
            "type": "string",
            "minLength": 1,
            "description": "The module API version this code was authored and reviewed against. A contract change invalidates prior reviews rather than silently running old code against new expectations."
          },
          "reviewed_by": {
            "type": "string",
            "minLength": 1,
            "description": "User id or email of the human reviewer (PRD Q7). Exempt from R12: this is provenance for a security decision, not org-specific scan input, so it is a literal by design and must NOT become a context ref."
          },
          "reviewed_at": {
            "type": "string",
            "format": "date-time",
            "description": "When the review recorded above was performed."
          }
        }
      }
    }
  },
  "additionalProperties": false,
  "$comment_additional_properties": "CLOSED at the envelope, OPEN inside every section — changed 2026-08-03 in answer to aeo-agent-service, who pointed out that root-level `additionalProperties: true` converted three of their four conformance defects into silence rather than errors. The worst was `productDescription` (camelCase): accepted, ignored, and `product_description` simply stayed unset — no error, no data. That is the failure mode this contract exists to prevent. The envelope is declared LOCKED in this schema's own description, so an undeclared root key is now an error; section internals stay `additionalProperties: true` because they are genuinely still being co-designed with the scanner workstream. CASING: every key in this contract is snake_case. Emit snake_case; a camelCase key is now a hard error instead of silent data loss. TRADE-OFF ACCEPTED: additive root fields now need a coordinated version bump rather than arriving free. That is the intended cost — the version policy called additive change cheap, but 'cheap' is worthless if a typo is indistinguishable from a new field.",
  "$defs": {
    "section": {
      "type": "object",
      "additionalProperties": true,
      "description": "AUTHORING section envelope — one of the five fixed conversation steps the chat proposes and the user accepts individually. Top-level in THIS document (`geography`, not `sections.geography`). Internals intentionally open for v1 co-design; strict-mode enforcement of section internals lands once the runtime workstream ratifies each section's shape. NOT a runtime phase — see the root $comment.",
      "$comment": "THIS SCHEMA DOES NOT DEFINE STATE_DELTA POINTERS — see agui-state-envelope.json, which does. It describes the config *document*; AG-UI STATE_DELTA pointers address the *state envelope* that carries the document, so a config section is reached at `/draftConfig/geography`, not `/geography`. An earlier version of this description claimed the sections were left top-level so 'aeo-frontend's reducer pointers need no change' — that was WRONG on both halves: it conflated the two rootings, and its premise came from a frontend statement they themselves corrected on 2026-08-03 (their pointers are `/phases/<section>` against a `{metadata, phases, acceptance, modules, libraryMatch}` draft state, so sections were never top-level on their side). Do not infer a pointer root from this file."
    },
    "scoringAxis": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "max": {
          "type": "integer",
          "minimum": 0,
          "description": "Maximum points this axis can contribute. Only the ceiling is declared here — how the axis earns its points is engine-owned and vertical-neutral."
        }
      },
      "description": "One scoring axis's point ceiling. Deliberately declares `max` ONLY: the ceiling is a weight and is vertical-neutral, whereas an axis's internals (keyword tables, stage lists, region maps) are exactly the industry-specific content the engine is being freed from. additionalProperties stays true so an engine-owned internal still validates while it exists."
    },
    "contextRef": {
      "type": "object",
      "required": ["context_ref"],
      "properties": {
        "context_ref": {
          "type": "string",
          "description": "Well-known org context-field key resolved at scan time from the org's runtime context (WELL_KNOWN_ORG_INPUTS). This is how one skill serves many orgs (R12)."
        },
        "default": {
          "description": "Optional documented default literal, permitted only adjacent to a context_ref."
        }
      },
      "additionalProperties": false,
      "description": "Org-agnostic binding marker. Org-specific dimensions reference a context field instead of a literal. The R12 org-coupling lint (code-side, not pure JSON Schema) walks the config to flag bare literals in org-specific positions."
    },
    "boundStringList": {
      "anyOf": [
        { "$ref": "#/$defs/contextRef" },
        { "type": "array", "items": { "type": "string" } }
      ],
      "description": "An R12-bound list dimension: either a context binding or a bare string array. Deliberately permits the literal — the ORG-COUPLING LINT rejects it, not this schema. Two reasons: the lint reports `{location, message, kind: 'org_coupling'}` which is a far better error than a JSON Schema anyOf failure, and it can distinguish an enforced position from a merely-similar one. What this $def adds is TYPE safety, which nothing had: a number, a nested object or a `{context_ref: 123}` at one of these positions previously reached the runtime unchallenged."
    },
    "discoverySource": {
      "type": "object",
      "required": ["name_field", "fields", "queries"],
      "additionalProperties": true,
      "properties": {
        "name_field": {
          "type": "string",
          "description": "Which key in a returned record holds the company name."
        },
        "fields": {
          "type": "array",
          "items": { "type": "string" },
          "minItems": 1,
          "description": "Fields the model is asked to return per record."
        },
        "queries": {
          "type": "array",
          "items": { "type": "string" },
          "minItems": 1,
          "description": "Search templates, passed to the model VERBATIM. `{market}` is expanded per target market by the scanner before dispatch. A template with no placeholder is passed through once unchanged and carries NO geography — legitimate for a national registry, and the single most expensive mistake in this repo's history when unintentional. Not schema-enforceable either way: `aeo/phases/query_expansion.py` supports both deliberately, so requiring the placeholder would forbid the legitimate case.",
          "$comment_war_story": "The unintentional case, in full: Austin zips in, Dallas firms out, three runs running. ⚠️ MOVED OUT OF `description` 2026-08-10 (thread #28) AND THIS IS A RULE, NOT TIDYING: aeo-agent-service RENDERS `description` FIELDS INTO THE MODEL'S PROMPT (verified 2026-08-12 — their finding, and it is theirs alone; the scanner does NOT read these descriptions, it builds prompts from RESOLVED CONFIG VALUES via `build_prompt`/`phases/validation.py`, which is a different hazard and the reason a literal reaching `default` is live behaviour rather than merely untidy). That parenthetical was the ONLY city literal in the model's entire instruction set, and it anchored a whole invented Austin metro into authored configs — `home_markets: [\"Austin, TX\"]`, plus `Round Rock` and `Pflugerville`, both Austin suburbs, in an org whose address is Charlotte, NC. A memorable proper noun in a rendered field is training data. **Never put a place name, company name, person, or any other concrete proper noun in a `description` here — put it in a `$comment*` key, which is not rendered.** Prose about behaviour is fine and wanted; a specific example that a model can copy is not."
        },
        "seed_firms": {
          "not": {},
          "description": "FORBIDDEN — do not author this. The runtime populates it by fanning out `discovery.lookalike_sources`; see context-field-keys.json's `runtime_populated_positions`."
        }
      },
      "$comment": "The engine's own source shape, vendored verbatim — `SOURCE_REQUIRED_KEYS = (\"name_field\", \"fields\", \"queries\")` at config_mapping.py:36. `additionalProperties: true` because these buckets pass through to a vendored engine we do not own; closing them would reject engine keys we have not enumerated. ⚠️ `seed_firms` uses `not: {}` (matches nothing, so the key's mere presence fails) rather than omission — it is the second, independent enforcement of the library invariant R12 exists to protect: seed firms are the commissioning org's own customers, and authored as a literal here the NEXT org to connect the skill searches using the first org's customer list. Previously lint-only; a literal here passed this schema, and a model reaches for it because a bare list is the obvious way to express \"firms we already know\"."
    },
    "geographyTargeting": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "use_zip_discovery": {
          "type": "boolean",
          "description": "Opt in to the zip-discovery phase. FALSY MEANS SKIP — the phase is off unless this is explicitly true, even when `zip_discovery` is in the dispatched subset."
        },
        "max_zips_per_market": {
          "type": "integer",
          "minimum": 1,
          "description": "Cap on zips discovered per target market."
        },
        "max_search_zips": {
          "type": "integer",
          "minimum": 1,
          "description": "Cap on discovered zips actually searched (runtime default 10). Allocation round-robins across markets so secondary areas are not starved."
        },
        "geo_strictness": {
          "type": "string",
          "enum": ["metro", "state"],
          "description": "How a prospect's verified location is judged against the target area. `metro` (runtime default) rejects same-state-but-different-metro; `state` accepts anywhere in state."
        },
        "max_queries_per_source": {
          "type": "integer",
          "minimum": 1,
          "description": "Cap on expanded queries per discovery source (runtime default 12). Templates × markets multiplies fast and each expansion is a grounded search."
        },
        "max_discovery_rounds": {
          "type": "integer",
          "minimum": 1,
          "description": "Cap on discovery sweep rounds."
        }
      },
      "$comment": "Shape ratified 2026-08-04 (thread #17) against what `aeo/runner.py` actually reads. Every knob is OPTIONAL and the runtime carries its own default for each, so this $def exists purely for TYPE enforcement — which nothing had: `max_search_zips: \"10\"` or `geo_strictness: \"strict\"` previously reached the scanner, where the first is `int()`-coerced by luck and the second silently falls back to `metro`. `geo_strictness` is the only enumerable one; the values are STRICTNESS_METRO/STRICTNESS_STATE at geo_filter.py:136-137."
    }
  }
}
