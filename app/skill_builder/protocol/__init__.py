"""AG-UI protocol handling — contained to this one package (PRD §4, Risk: AG-UI
pre-1.0). Nothing outside `app.skill_builder.protocol` should construct or
parse AG-UI event shapes; import from `agui` instead.
"""
