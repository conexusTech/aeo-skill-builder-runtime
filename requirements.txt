# Pinned exactly, mirroring aeo-groundtruth-browser-runtime. This image is deployed
# to AgentCore Runtime and rebuilt rarely, so a floating dependency means the thing
# that changed between a working and a broken ARM64 build is invisible in the diff.
#
# Versions are the ones the code was developed and tested against in
# aeo-agent-service, read off that environment rather than chosen fresh.
fastapi==0.115.14
uvicorn[standard]==0.34.3
pydantic==2.12.5
pydantic-settings==2.13.1

# Config-contract validation and STATE_DELTA patch construction.
jsonschema==4.26.0
jsonpatch==1.33

# The Bedrock Mantle client. Imported LAZILY inside BedrockChatModel, so the module
# and the whole test suite import without it — but the runtime genuinely needs it.
#
# ⚠️ Do not grep for `import anthropic` to decide whether this is used: every import
# here is of the `from anthropic import X` form, which does not contain that string.
#
# 🔴 THE `[bedrock]` EXTRA IS LOAD-BEARING — it pulls boto3/botocore, which
# AnthropicBedrockMantle needs for SigV4 signing. Without it the FIRST model-backed
# turn dies with `ModuleNotFoundError: No module named 'botocore'`, and it did:
# this file originally said plain `anthropic==0.120.0`, derived by grepping the
# package's import statements. botocore appears in no import here — it is a
# transitive runtime need of the Bedrock client — so the grep could not have found
# it. In aeo-agent-service the gap was invisible because boto3 was a direct
# dependency there for the geo-local ground-truth work.
anthropic[bedrock]==0.120.0
